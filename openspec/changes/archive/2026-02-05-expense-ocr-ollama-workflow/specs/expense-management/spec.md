## ADDED Requirements

### Requirement: Receipt-First Creation Workflow
The expense creation process SHALL be initiated by uploading a receipt image, which triggers an automatic analysis.

#### Scenario: Starting Expense Creation with Upload
- **WHEN** a user selects "Add Expense" and uploads an image
- **THEN** the system immediately starts the analysis process before showing the full form
